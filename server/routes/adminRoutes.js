import { Router } from 'express';import { protect,adminOnly } from '../middleware/auth.js';import { uploadCover } from '../middleware/upload.js';import * as a from '../controllers/adminController.js';
const r=Router();r.use(protect,adminOnly);r.get('/dashboard',a.dashboard);r.get('/novels',a.adminNovels);r.post('/novels',uploadCover,a.createNovel);r.put('/novels/:novelId',uploadCover,a.updateNovel);r.patch('/novels/:novelId/publish',a.toggleNovel);r.delete('/novels/:novelId',a.deleteNovel);r.get('/novels/:novelId/chapters',a.chapters);r.post('/novels/:novelId/chapters',a.createChapter);r.put('/chapters/:chapterId',a.updateChapter);r.patch('/chapters/:chapterId/publish',a.toggleChapter);r.delete('/chapters/:chapterId',a.deleteChapter);r.get('/users',a.users);r.patch('/users/:userId/ban',a.ban);r.patch('/users/:userId/unban',a.unban);r.delete('/users/:userId',a.deleteUser);r.get('/comments',a.comments);r.patch('/comments/:commentId/approve',a.approve);r.delete('/comments/:commentId',a.deleteComment);export default r;
